import { Results } from './Results';
import { render, screen } from '@testing-library/react';

describe('Results component', () => {
  test.todo('should render a message if no games in store', () => {
    render(<Results />);
  });
  test.todo(' should show date and a list of games');
});
