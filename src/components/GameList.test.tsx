import { GameList } from './GameList';
import { render, screen } from '@testing-library/react';

describe('GameList component', () => {
  test.todo('should render two li items', () => {
    render(<GameList />);
  });

  test.todo('should contain a header title Game List', () => {});
});
